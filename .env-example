# Deployment configuration
REMOTE_HOST=root@192.168.0.10
REMOTE_PATH=/opt/nas_monitor

# Application configuration (optional overrides)
# NAS_DB_PATH=sqlite://data/metrics_db.sqlite3
# NAS_COLLECTOR_INTERVAL_CPU=5
# NAS_COLLECTOR_INTERVAL_RAM=5
# NAS_COLLECTOR_INTERVAL_NETWORK=3
# NAS_COLLECTOR_INTERVAL_STORAGE=60
# NAS_COLLECTOR_INTERVAL_ZFS_POOL=600
# NAS_API_HOST=0.0.0.0
# NAS_API_PORT=8000
# NAS_CORS_ORIGINS=["*"]
# NAS_DISABLE_TASKS=false
# Frontend configuration
FRONTEND_PORT=9000
